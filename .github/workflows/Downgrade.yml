name: Downgrade
env:
  JULIA_NUM_THREADS: 4
  JULIA_PKG_PRECOMPILE_AUTO: 0
on:
  pull_request:
    branches:
      - main
      - dev
    paths-ignore:
      - 'docs/**'
  push:
    branches:
      - main
      - dev
    paths-ignore:
      - 'docs/**'
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: ['1.10', '1']
    steps:
      - uses: actions/checkout@v5
      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
      - name: Add LegendJuliaRegistry
        run: julia -e 'using Pkg; Pkg.Registry.add("General"); Pkg.Registry.add(Pkg.RegistrySpec(url = "https://github.com/legend-exp/LegendJuliaRegistry"))'
        shell: bash
      - name: Restrict Geant4_jll to 11.3.2 # Temporary fix: this is needed until Geant4.jl is compatible with Geant4_jll@11.4
        run: julia --project=. -e 'using Pkg; Pkg.add(Pkg.PackageSpec(name="Geant4_jll", version="11.3.2")); Pkg.compat("Geant4_jll", "=11.3.2")'; rm Manifest.toml
        shell: bash
      - name: Downgrade NearestNeighbors # Temporary fix: this is needed to work with older versions of StaticArrays
        run: julia --project=. -e 'using Pkg; Pkg.add(name = "NearestNeighbors", version = "0.4.13"); Pkg.compat("NearestNeighbors", "0.4.13")'
        shell: bash
        if: matrix.version == '1.10'
      - uses: julia-actions/julia-downgrade-compat@v2
        with:
          julia_version: ${{ matrix.version }}
          mode: deps
          skip: Pkg,TOML
      - uses: julia-actions/julia-buildpkg@v1
      - uses: julia-actions/julia-runtest@v1
        with:
          coverage: false
